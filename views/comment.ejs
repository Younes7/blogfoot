<%include ./partials/header%>
<% com.forEach(function(item){ %>
<div class="container">
  <div class="row">
    <div class="col-md">
      <p>Nom : <%= item.name %></p>
      <p>Email: <%= item.email %></p>
      <p>Date: <%= item.date %></p>
      <p>Commentaire : <%= item.comment %></p>
    </div>
  </div>
</div>
<%}) %>
